These are Claude Design canvas artboards (.dc.html) for mnr.network — not a runnable website.
Use them as the visual and copy reference when building the real static site
(same tokens, same sections, light/dark via the CSS variables in the <helmet> block).
